const os = require("os");

// console.log("os===arch==>",os.arch());
// console.log("os===free mem==>", os.freemem() / (1024 * 1024 * 1024));
// console.log("os===total mem==>", os.totalmem() / (1024 * 1024 * 1024));
// console.log("os===host name==>", os.hostname());
// console.log("os===platform==>", os.platform());
console.log("os===user infog654321q wertyui907==>", os.userInfo());
